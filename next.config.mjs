/* Served at the domain root: https://sanyamunam.com
   (GitHub Pages redirects sanyamunam.github.io/portfolio here) */
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  trailingSlash: true,
};

export default nextConfig;
